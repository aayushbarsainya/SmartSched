#pragma once
#include <cstdint>

namespace CustomTasks {
    // Return computed values for display; heavy CPU to simulate work
    std::uint64_t kthPrime(int k);
    std::uint64_t fibonacciK(int k);
    std::uint64_t factorialK(int k);
    std::uint64_t kthPalindrome(int k);
}


